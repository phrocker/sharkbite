[build-system]
requires = ["setuptools==75.8.0", "wheel==0.45.1"]
build-backend = "setuptools.build_meta"

[project]
name = "shoal-sharkbite"
version = "1.3.0"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
